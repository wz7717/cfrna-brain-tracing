# BrainTrace CI / release-gate environment
# =========================================
# The full reproduction file already pins every runtime dependency plus
# pytest and coverage.  This compatibility entry point intentionally adds no
# conflicting duplicate pins.
-r requirements_reproducible.txt
